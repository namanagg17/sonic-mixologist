# Server Configuration (Optional)
NODE_ENV=development
PORT=3001
CORS_ORIGIN=http://localhost:5173

# Note: Sonic Mixologist now runs entirely client-side
# This server is optional and only provides health check
# No Spotify or external API credentials needed
